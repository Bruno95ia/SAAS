[pytest]
# só roda os testes da pasta tests/
testpaths = tests
# adiciona src ao PYTHONPATH para "from saas..." funcionar
pythonpath = src
# ignora diretórios que não queremos varrer
norecursedirs = cvat data datasets runs .venv venv env
# flags padrão (opcional)
addopts = -q